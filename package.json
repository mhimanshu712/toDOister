{
  "name": "electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "packwin": "electron-packager . electron-tutorial-app --overwrite --asar=true --platform=win32 --arch=ia32  --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"FirstBlood\""
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "electron": "^9.4.0",
    "electron-store": "^5.1.1"
  },
  "devDependencies": {
    "electron-packager": "^14.2.1"
  }
}
